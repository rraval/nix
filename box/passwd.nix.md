This file defines a nix expression to store hashed local passwords, like:

```
{
  "rraval" = "<hash>";
}
```

The key should be the user name and the hash can be generated by:

```
$ mkpasswd -m sha-256
```
